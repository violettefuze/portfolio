# Version Archive

Each version should have:

- a git tag such as `v0.3.0`
- a short note file in this folder
- a homepage snapshot in `docs/versions/assets/`

The tag is the rollback name. The note is the human summary. The screenshot is the visual memory.
